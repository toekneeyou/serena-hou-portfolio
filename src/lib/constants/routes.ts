export enum ROUTES {
  HOME = "",
  PROJECT = "projects",
  VISUAL = "visuals",
  VIDEO = "videos",
  ABOUT = "about",
  CONTACT = "contact",
}

export enum ABS_ROUTES {
  HOME = `/${ROUTES.HOME}`,
  PROJECT = `/${ROUTES.PROJECT}`,
  VISUAL = `/${ROUTES.VISUAL}`,
  VIDEO = `/${ROUTES.VIDEO}`,
  ABOUT = `/${ROUTES.ABOUT}`,
  CONTACT = `/${ROUTES.CONTACT}`,
}
